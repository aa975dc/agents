"""服务层：请求级缓存、分维度覆盖账等进程内支撑设施，及文件归属台账（ownership）。

边界：不跨进程、不跨线程共享；作用域外的行为与无缓存完全一致。例外：
ownership 台账在显式给 store_path 时原子落盘（进程重启后归属可恢复）。
campaign 深读活动账本同为跨活动设施：检查点原子落盘，暂停/续跑跨进程一致。
resume 台账（ResumeLedger）跨会话登记活动与续接动作：epoch 围栏防僵尸双写，
锚变更/检查点缺失如实标注，不做自动唤醒（P6-04）。
"""
