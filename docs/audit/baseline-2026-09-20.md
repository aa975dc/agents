# P0-01 基线与证据（2026-09-20）

任务 P0-01（仓库身份、差异与本地测试基线）。本文件为实际执行记录，不是计划。

## 1. 仓库身份与差异映射

| 项 | 值 |
|---|---|
| 本地工作仓库 | /Users/youxididai/Documents/cj/agents（remote: git@github.com:aa975dc/agents.git） |
| 方案基线 | 204a9146f3fc9964960a1476ab25f0e22c116159 |
| 当前 HEAD | 204a9146f3fc9964964960a… **完全一致**（`git rev-parse HEAD` 实测），无需差异映射 |
| 分支 | main → 新建工作分支 optimization-v1（未动 main，不 push） |
| 未提交修改 | 仅 1 个未跟踪 `.DS_Store`（用户已有内容，保留不动）；无已跟踪文件修改、无 stash |

结论：当前版本与方案基线相同，不存在"方案基于旧代码"的漂移；用户已有工作（.DS_Store）未被覆盖。

## 2. 环境与宿主取证

| 项 | 值 |
|---|---|
| Python | 3.9.6（与 README 声明 ≥3.9 一致） |
| Node | v24.19.0 |
| Git | 2.50.1 (Apple Git-155) |
| OS | macOS 26.2, arm64 |
| 插件缓存 | ~/.zcode/cli/plugins/cache/aa975dc-agents/：dev-companion 0.1.0/0.1.1/0.1.2/0.2.0；code-analysis-swarm 0.2.0/0.2.1 |
| 宿主实际加载证据 | 本 ZCode 会话的 Agent 工具列表中 code-analysis-swarm 的 a1–a7 七个代理与 dev-companion 的 companion-developer/companion-checker 真实可用（2026-09-20 会话内实际调用过 a1–a7 并返回结果）——插件角色在真实宿主加载成功的直接证据 |

## 3. 测试脚本安全检查（隔离实跑前置）

grep 全部 tests/*.py 与 *.mjs：无网络调用（socket/urllib/requests 零命中）；subprocess 仅以 `sys.executable` 调用本仓库 scripts；unlink/rmtree 仅作用于 tempfile 生成的 fixture；test_releases.py 自述 "disposable local projects and local subprocesses only"。判定：可安全在隔离目录实跑。

## 4. 隔离实跑基线（/tmp/p0-iso，独立克隆，非用户工作树）

| 套件 | 命令 | 结果 |
|---|---|---|
| Python unittest | `python3 -m unittest discover -s tests -q` | **Ran 106 tests — OK**（3.565s） |
| Node（mock 宿主） | `node --test tests/swarm_workflow.test.mjs` | **36 pass / 0 fail**（71.9ms） |
| 生命周期演示 | `python3 -B dev-companion/examples/lifecycle_demo.py` | **exit 0** |

分项口径（按 01_COMBINED_DECISIONS 校正）：106 为真实文件系统级 unittest；36 为 **mock 宿主**单测（不证明 DWF 真实编译/运行）；演示 1 项。合计 143 项通过 + demo，与 README 自报的 106/36/4 中的前两类一致（demo 计法不同，不虚报）。

## 5. 已知宿主编译证据（P0-02 探针，详见 docs/host-contract/）

2026-09-20 在真实 ZCode 宿主以 CreateWorkflow 编译入口实测：
- 反例 1（H01）：`phase(stageName)`（运行时变量）→ **编译拒绝**，诊断："phase()'s argument must be a compile-time string literal"。
- 反例 2（H02）：`world.run(cmd, …)`（变量命令名）→ **编译拒绝**，同规则。
- 正例（字面量 phase + 字面量 world.run + artifact 探针）：编译通过，运行结果见 docs/host-contract/host-probe-2026-09-20.md。

影响：Z01 的证据等级从 static_confirmed_host_unverified 升级为 **host_compile_rejected_confirmed**——code-analysis-swarm/workflow/code-analysis.dwf.ts 的六处 `phase(stage)` 在真实宿主必然无法编译。

## 6. 遗留与下一批

- H04/H05 正例探针运行中（结果落地后补 docs/host-contract/）。
- 下一批：P1-01（DWF phase 字面量 + 测试分层）、P1-03（特殊文件/孤立状态）可并行；P1-02 需先落 helper 契约。
